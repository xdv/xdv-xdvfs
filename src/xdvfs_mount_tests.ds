// XDVFS - Mount Tests

forge XdvFsMountTests {

    proc K::test_mount_flags() {
        let rdonly = 1;
        let noexec = 2;
        let nosuid = 4;
        let nodev = 8;
        let sync = 16;
        return 0;
    }

    proc K::test_mount() {
        let result = 0;
        return 0;
    }

    proc K::test_unmount() {
        let result = 0;
        return 0;
    }

    proc K::test_remount() {
        let result = 0;
        return 0;
    }

    proc K::test_sync_filesystem() {
        let result = 0;
        return 0;
    }

    proc K::test_check_filesystem() {
        let valid = 1;
        return 0;
    }

    proc K::test_format_filesystem() {
        let result = 0;
        return 0;
    }

    proc K::test_get_fs_stats() {
        let handle = 1;
        return 0;
    }
}
